# Backward compatibility: re-export from new module locations
# The original main.py imports from utils.detection, utils.embedding, utils.similarity
# These still work via the original files in this directory.
